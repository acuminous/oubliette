const bufferFormat = require('./buffer-format');
const jsonFormat = require('./json-format');
const rawFormat = require('./raw-format');
const stringFormat = require('./string-format');

module.exports = {
  bufferFormat,
  jsonFormat,
  rawFormat,
  stringFormat,
};
